# /etc/nginx/sites-available/pomodoro.works.conf
server {
    if ($host = www.pomodoro.works) {
        return 301 https://$host$request_uri;
    } # managed by Certbot

    listen 443;
    listen [::]:443;
    
    server_name pomodoro.works www.pomodoro.works;

    location / {
        #try_files $uri $uri/ =404;
        proxy_pass http://localhost:4001;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }

    ssl_certificate /etc/letsencrypt/live/api.pomodoro.works/fullchain.pem; # managed by Certbot
    ssl_certificate_key /etc/letsencrypt/live/api.pomodoro.works/privkey.pem; # managed by Certbot
}

server {
    if ($host = pomodoro.works) {
        return 301 https://$host$request_uri;
    } # managed by Certbot
   
    listen 80;
    listen [::]:80;

    server_name pomodoro.works www.pomodoro.works;
    return 404; # managed by Certbot
}
