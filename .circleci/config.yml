version: 2

 jobs:
   #build:`dd
   #  #...
   deploy:
     machine:
       enabled: true
     steps:
       - run:
           name: Deploy Over SSH
           command: |
             ssh $SSH_USER@$SSH_HOST "cd projects/pomodoro && git pull && GOOGLE_CLIENT_ID=547377203778-rnas01ng8irns54hpho81roosuoagucq.apps.googleusercontent.com"

 workflows:
   version: 2
   build-and-deploy:
     jobs:
       - build
       - deploy:
           requires:
             #- build # only deploy once build job has completed
           filters:
             branches:
               only: master # only deploy on the master branch
